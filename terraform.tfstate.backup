If we ever remove the terraform state backup, terraform will not be able to manage our resources anymore
because the file that terraform is using to track all our resources is deleted. the terraform state backup is store into our local machine, and terraform use it to track and manage our resource
If we delete the tearraform state backup, the entire infrastrucure that we have in AWS will be useless.


State backend in terraform
It is a file
